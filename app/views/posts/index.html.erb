<div class="row">
  <% for post in @posts %>
    <div class="col-md-3 col-sm-6">
      <img src="<%= post["image"] %>" class="img-fluid">
      <p><%= post["body"] %></p>
    </div>
  <% end %>
</div>